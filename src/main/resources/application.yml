server:
  port: 8082
spring:
  datasource:
#    url: jdbc:mysql://localhost:3306/quora
    url: jdbc:mysql://172.17.0.2:3306/quora-db
    username: root
    password: root
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5InnoDBDialect
    hibernate:
      ddl-auto: update
    generate-ddl: true
    show-sql: true

jwt:
  secret: quora-app

